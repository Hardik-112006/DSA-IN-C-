//void *is a generic pointer that can hold the address of any data type,
// but because it doesn't know the type of the object it points to, 
//you must cast it to the correct pointer type before accessing the value.